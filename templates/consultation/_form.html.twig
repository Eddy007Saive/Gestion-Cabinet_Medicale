<div class="bg-white p-4 shadow-lg rounded-2 ">
    {{ form_start(form) }}
    <div class="d-flex bg-white  gap-4  justify-content-space-between">
        <div class="w-50  p-3 shadow-lg rounded-2">
            <div class="form-group">
                <label for="" class="form-label">Médecin</label>
                {{ form_widget(form.ID_MEDECIN) }}
                {{ form_errors(form.ID_MEDECIN) }}
            </div>
            <div class="form-group">
                <label for="" class="form-label">Date :</label>
                {{ form_widget(form.DATE_CONS) }}
                <div class="form-error   text-danger">
                    {{ form_errors(form.DATE_CONS) }}
                </div>
            </div>
        </div>

        <div class="form-group  w-50 p-3 shadow-lg rounded-2 ">
            <label for="" class="form-label">Patient</label>
            {{ form_widget(form.ID_PATIENT) }}
            <div class="form-error  text-danger">
                {{ form_errors(form.ID_PATIENT) }}
            </div>
        </div>

    </div>



    <div class="mt-3">
        <div class="form-group">
            <label for="" class="form-label">Symptome</label>
            {{ form_widget(form.SYMPTOME) }}
            <div class="form-error   text-danger">
                {{ form_errors(form.SYMPTOME) }}
            </div>
        </div>
        <div class="form-group">
            <label for="" class="form-label">Diagnostic</label>
            {{ form_widget(form.DIAGNOSTIC) }}
            <div class="form-error   text-danger">
                {{ form_errors(form.DIAGNOSTIC) }}
            </div>
        </div>


        <div class="form-group">
            <label for="" class="form-label">Liste des medicaments</label>
            {{ form_widget(form.MEDICAMENTS) }}
            <div class="form-error  text-danger">
                {{ form_errors(form.MEDICAMENTS) }}
            </div>
        </div>
    </div>
    <div class="d-flex justify-content-center">
        <button class="btn btn-primary mt-4 ">{{ button_label|default('Enregistrer') }}</button>
    </div>
    {{ form_end(form) }}

</div>