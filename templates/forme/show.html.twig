{% extends 'base.html.twig' %}

{% block title %}Forme{% endblock %}

{% block body %}
    <h1>Forme</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ forme.id }}</td>
            </tr>
            <tr>
                <th>FORME</th>
                <td>{{ forme.FORME }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_forme_index') }}">back to list</a>

    <a href="{{ path('app_forme_edit', {'id': forme.id}) }}">edit</a>

    {{ include('forme/_delete_form.html.twig') }}
{% endblock %}
