<form method="post" action="{{ path('app_specialite_delete', {'id': specialite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <button class="btn btn-danger mt-4 ">Delete</button>
</form>
